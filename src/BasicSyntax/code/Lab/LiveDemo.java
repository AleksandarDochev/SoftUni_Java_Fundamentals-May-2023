package BasicSyntax.code.Lab;

import java.util.Scanner;

public class LiveDemo {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        //System.out.println("Enter stuff here for num and other stuff  \n num1 (double)\n num (int) \n prcentage int \n grade int");

//        System.out.println("name:");
//        String name = scanner.nextLine();
//        System.out.println("age:");
//        int age = Integer.parseInt(scanner.nextLine());
//        String resultText2 = String.format("%s, %d",name, age);
//        System.out.println(resultText2);

        //here we create a string with data "Alex ," and we add age as an additional value// from the console
        //String resultText = String.format("Alex, %d", age);
        //System.out.println(resultText);

        //double numDouble = Double.parseDouble(scanner.nextLine());
        //int numInt = Integer.parseInt(scanner.nextLine());
        //int percentage = Integer.parseInt(scanner.nextLine());
        //int grade = Integer.parseInt(scanner.nextLine());


        //double floatingNum = Double.parseDouble(scanner.nextLine());

        //System.out.printf("Random text balala here your number  , %f", num1);
        //System.out.printf("%03d  here we put a zero infront so we have 3 numbers at all times despite the number we initially give  ",numInt);
        //System.out.printf("%.2f  <here we turn numbers with 2 points after the comma " ,numDouble);



    }
}
